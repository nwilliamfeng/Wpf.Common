<UserControl x:Class="Wpf.Common.Demo.Controls.AssistButtonGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf.Common.Demo.Controls"
             xmlns:controls="clr-namespace:Wpf.Common.Controls;assembly=Wpf.Common.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"  >
    <UserControl.Resources>

    </UserControl.Resources>
    <Grid Background="Transparent">
        <controls:AssistButtonGroup ItemsSource="{Binding Source=123456789}" VerticalAlignment="Center" HorizontalAlignment="Center">
            <controls:AssistButtonGroup.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Padding="15,8" Style="{StaticResource FlatButtonStyle}" Background="LightGray"/>
                </DataTemplate>
            </controls:AssistButtonGroup.ItemTemplate>
        </controls:AssistButtonGroup>
    </Grid>
</UserControl>
